<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :company_name %>
  </div>

  <div class="field">
    <%= form.label :logo %>
    <%= form.text_field :logo, id: :company_logo %>
  </div>

  <div class="field">
    <%= form.label :datestamp %>
    <%= form.text_field :datestamp, id: :company_datestamp %>
  </div>

  <div class="field">
    <%= form.label :people %>
    <%= form.number_field :people, id: :company_people %>
  </div> prompt: "Competence"

  <div>
    <%= form.select(:competence_ids, nested_dropdown(Competence.all.arrange), prompt: "Competence", selected: @competence ) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
